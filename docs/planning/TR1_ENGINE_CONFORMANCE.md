# ТР-1 2026 — Engine Conformance (rule → IMPLEMENTED / DIVERGES / MISSING)

> **What this is.** A per-rule audit of the SimpleCargo tariff engine against the verbatim ТР-1 2026 calc order (rulebook chunk [`rulebook/prilozhenie-n-1-ii.md`](./rulebook/prilozhenie-n-1-ii.md) + verbatim quotes in [`TARIFF_RULES_EXACT.md`](./TARIFF_RULES_EXACT.md) §2–§5 + machine seed [`scripts/seed-data/tr1-rounding-rules.json`](../../scripts/seed-data/tr1-rounding-rules.json)). Index of all rules: [`TR1_RULEBOOK.md`](./TR1_RULEBOOK.md).
>
> **Engines audited (all under `src/lib/tariff/`):**
> - `computeTariffN8.ts` — the **certified** own-полувагон N8 contour (the golden-oracle path).
> - `computeTariff.ts` — the **orchestrator** (routes certified contour; has a universal/container/cistern fallback).
> - `computeInventory.ts` — the **общий-парк (inventory)** path (N8 груж + N25(1)@60% + В), self-flagged «НЕ ВЫВЕРЕНО».
>
> **Top-line verdict (updated 2026-06-09).** The certified N8 chain is a faithful, kopeck-exact transcription of пп.16.5→16.9 + the 16.7.3 max-of-two + 15.4/15.5 rounding + 18.1.1/18.2 — **IMPLEMENTED**. The orchestrator honors scheme/coefficient order and routes the certified contour correctly. The three previously-DIVERGING gaps are now **CLOSED**: (1) **п.15.4 per-step kopeck rounding** is now applied on the **universal fallback**, the **цистерна per-tonne** rate, the **k4BaseDelta** candidates in `computeTariff.ts`, and on all three legs (груж/порож/В) of `computeInventory.ts` — each `×coefficient` is now `round01`-ed, mirroring the certified `computeTariffN8.ts` chain, and proven a kopeck-exact no-op vs every oracle. (2) **п.16.10 Табл.N12/N13 reductions** are now sourced verbatim (`scripts/seed-data/tr1-reductions.json`) and the **Табл.N12 FCL container reduction** is wired into the container path (subtracted at целые-копейки before the п.15.5 round; YELLOW-flagged when the size→Табл.N10 row mapping is not verbatim). (3) The **−754 ₽** inventory discount is now resolved: it is **proven FLAT** (753,86/754,32 bracket 754 with opposite K4 sign) and re-cited honestly as the R-Тариф «Скидка с общего тарифа на универсальные вагоны» conformance constant (corroborated-by-oracle, NOT a numbered ТР-1 reduction) — see [`INVENTORY_754_RESOLUTION.md`](./INVENTORY_754_RESOLUTION.md). **Табл.N3 directional** is now a sourced seed (`tr1-k3-directional.json`) with a documented ×1.0 no-op for ordinary RF directions. **Residual:** п.16.10 reductions are not yet wired into the **контрейлер** path (needs Табл.N11 base) and the **−754** remains corroborated-by-oracle rather than rule-derived. **No fabricated numbers** — every coefficient traces to a sourced rule constant or an oracle-corroborated value flagged as such.

---

## A. Previously-DIVERGING / MISSING gaps — now CLOSED (with the residual stated)

> All five rows below were DIVERGES/MISSING in the prior audit. After the 2026-06-09 effort each is **IMPLEMENTED** or **RESOLVED**; the honest residual is in the rightmost column. Verified against the full gate: `vitest run src/lib/tariff src/lib/distance` → **238 passed (18 files)**, `tsc --noEmit` → exit 0; all 4 certified oracles + 13 batch cases + 3 distances still EXACT.

| Sev | Rule | New verdict | Engine ref (after fix) | What closed it + honest residual |
|---|---|---|---|---|
| **HIGH** | **п.16.10 / 15.5** — subtract Табл.N12/N13 reductions **before** the whole-ruble round | **IMPLEMENTED (Табл.N12 container) / SOURCED (Табл.N13 контрейлер)** | `computeTariff.ts` container branch: `resolveContainerReduction(...)` → `round2(Math.max(0, plateWithIndex − reductionRub))` subtracted at п.16.10 before п.15.5; seed `scripts/seed-data/tr1-reductions.json` | Табл.N12 + Табл.N13 acquired verbatim (re-verified byte-for-byte against the live sudact HTML, T12=76629 / T13=73725 bytes). Table N12 (FCL container) is **wired**; subtraction is additive-ruble (not a multiplier), clamped ≥0, and **YELLOW-flagged** when the container size→Табл.N10 row mapping is not verbatim. **Residual:** Табл.N13 (контрейлер) is seeded but **not wired** — needs Табл.N11 (контрейлер base schemes, not on disk) to compute the base for subtraction. Inert for the щебень повагонная hot path (no reductions apply). |
| **HIGH** | **−754 ₽ inventory discount** — was an unjustified fitted lever | **RESOLVED — proven FLAT, re-cited honestly** | `computeInventory.ts` `INVENTORY_DISCOUNT = 754` with full provenance comment; `Math.round(loaded + emptyLeg + vLeg − 754)` | Proven **flat** across both inventory oracles: required reductions 753,86 (INV-1, +K4) and 754,32 (INV-6_20, −K4) **bracket** an integer 754 with **opposite K4 sign** → it cannot be a route-/K4-proportional formula. Ruled out as Табл.N12/N13/п.28.2 (those are container/контрейлер FCL only) and as any §II/п.18 clause. It is the R-Тариф «Скидка с общего тарифа на универсальные вагоны» that conforms the п.16.5.1 three-leg sum to the published combined И1 tariff. **Residual:** standing is **corroborated-by-oracle, NOT rule-derived** — to fully kill it, reproduce И1@1409/70т = 142911 analytically and show the offset is structural. Full analysis: [`INVENTORY_754_RESOLUTION.md`](./INVENTORY_754_RESOLUTION.md). |
| **MED** | **п.15.4** — округление до целых копеек **after each** ×coefficient (universal fallback) | **IMPLEMENTED** | `computeTariff.ts` universal branch: `iAcc` chain `round01(iBaseRate)→×k1→×k3→×k4→×innov→×classSurcharge→×dopIndex`, each `round01`; цистерна per-tonne `rpt` chain `round01` after each multiply; `k4BaseDeltaFactor` candCur/candPrev now `round01` | Replaced the single float product `iBaseRate*k1*k3*k4*innov*surcharge*dopIndex` with a per-step `round01` chain mirroring `computeTariffN8.ts:460–472`. Proven a **kopeck-exact no-op** vs every affected oracle (own-ПВ C2/C3, платформа, CIS-C3) before shipping. Residual: none for certified paths. |
| **MED** | **п.15.4** — same, on the inventory path | **IMPLEMENTED** | `computeInventory.ts`: loaded leg `round01(base+K4)→×k1→×0,77→×0,909→×1,01`; empty leg `round01(...)→×1,06→×1,01→×оси`; В leg `round01(vBase×1,01)` — each step `round01` | The previously-unused `round01` import is now applied on all three legs. Proven no-op vs INV-1 (110170) / INV-6_20 (105804) to the ruble. Residual: none. |
| **MED** | **порожний 60% vs actual** — 16.5.1 «60% по N25(1)» is **общий-парк only** | **CORRECT (unchanged)** | `computeInventory.ts` `POROZH_DISTANCE_FRACTION = 0.6`; `computeTariff.ts` own path uses full-distance N25, no 60%, no В | Still correct: 60% only on the inventory engine; собственный uses actual full-haul N25 and adds no В-component. Residual: keep the regression guard. |
| **LOW** | **Табл. N3 directional** vs **Табл. N4 commodity** are distinct (§I п.10) | **SOURCED + documented no-op** | `scripts/seed-data/tr1-k3-directional.json` (Табл.N3 split out from N4); resolver is a documented ×1.0 no-op when absent/empty | Табл.N3 is now its own seed (Калининград §1/§2 GREEN verbatim; named-route §4 YELLOW; погранстанции §3 RED/unverified — extractor unstable, NOT entered into the engine). For ordinary RF directions (incl. щебень) the directional multiplier is **1.0**, confirming zero effect on golden cases. **Residual:** §3 погранстанции coefficients need raw HTML / R-Тариф verification before any export-via-погранстык quote can apply them. |

---

## B. IMPLEMENTED — faithful transcriptions (no action)

| Rule | Verdict | Engine ref | Note |
|---|---|---|---|
| **п.15.4** per-step round to 0,01 ₽ (certified contour) | **IMPLEMENTED** | `computeTariffN8.ts:40` `round01()`, applied at `:444` (16.6), `:307/:315/:316` (16.7.1/.2), `:457` (16.8), `:460–462` (16.9), `:472` (innov) | Matches `tr1-rounding-rules.json.roundingSteps` clause-for-clause. |
| **п.15.4** per-step round to 0,01 ₽ (universal + цистерна fallback) | **IMPLEMENTED (2026-06-09)** | `computeTariff.ts` universal `iAcc` chain (`round01` after each of k1/k3/k4/innov/classSurcharge/dopIndex); цистерна per-tonne `rpt` chain; `k4BaseDeltaFactor` candCur/candPrev `round01` | Closes the prior universal-fallback DIVERGES; proven kopeck-exact no-op vs all affected oracles. |
| **п.15.4** per-step round to 0,01 ₽ (inventory path) | **IMPLEMENTED (2026-06-09)** | `computeInventory.ts` loaded/empty/В legs each `round01` per multiply | Closes the prior inventory DIVERGES; no-op vs INV-1/INV-6_20. |
| **п.16.10** Табл.N12 FCL container reduction subtracted before п.15.5 | **IMPLEMENTED (container path, 2026-06-09)** | `computeTariff.ts` `resolveContainerReduction()` → `round2(max(0, plate − reductionRub))`; seed `tr1-reductions.json` | Additive-ruble (not multiplier), clamped ≥0, YELLOW when size→Табл.N10 mapping not verbatim. Табл.N13 контрейлер seeded but not wired (needs Табл.N11). |
| **п.15.5** final ruble round, half-up, повагонная | **IMPLEMENTED** | `computeTariffN8.ts:45` `round1()` half-away-from-zero, used `:484`; orchestrator `computeTariff.ts:824` `Math.round`, cistern `:656` `round1` | `round1`/`Math.round` are half-up for positive tariffs = `roundMode.mode=half_up`. |
| **пп.16.1→16.4** order: расстояние → вид/тип/принадлежность → класс/ЕТСНГ → схема + коэффициенты | **IMPLEMENTED** | `computeTariff.ts` class/ЕТСНГ → distance → `resolveSchemes` (own/rzd → И/В/empty) precede rate lookup | Order matches the §II step map. |
| **п.16.5 / 16.5.1** общий-парк three-component sum (N8 груж + N25(1)@60% + В) | **IMPLEMENTED** | `computeInventory.ts:203` loaded(N8) + `:212–219` emptyLeg(25(1)@60%) + `:226` vLeg(В4/В1) | Three components present for общий парк. |
| **п.16.5.1** собственный/арендованный = NO В + full-distance N25 | **IMPLEMENTED** | `computeTariff.ts:8–9` header contract («В only if ownership='rzd'», «порожний only if own»); own path omits В, full-distance N25 | Exactly as rulebook §6 item 1 demands. Load-bearing — do not regress. |
| **п.16.6** K3 commodity as с-расстояния correction → round 0,01 | **IMPLEMENTED** | `computeTariffN8.ts:444` `baseK3 = round01(baseRate * C_K3_NERUD)` | Degenerate-from-km-1 case for нерудный 0,77 (see §C residual). |
| **пп.16.7.1/.2/.3 + 17.2** K4 max-of-two абс. величина + пояс floor | **IMPLEMENTED** | `computeTariffN8.ts:307` candCur, `:315–316` candPrev, signed max-of-two; mirrored in `computeTariff.ts:253–260`, `computeInventory.ts:151–157` | candPrev = 0 in first belt, matches п.16.7.2. |
| **п.16.8** add 16.7 correction onto 16.6 base → round 0,01 | **IMPLEMENTED** | `computeTariffN8.ts:457` `v = round01(baseK3 + k4r.correction)` | — |
| **п.16.9** sequential × K1 (Табл.2), нерудный ×0,909, own-полувагон class, innov ×0,9595 | **IMPLEMENTED** | `computeTariffN8.ts:460` ×k1, `:461` ×0,909, `:462` ×0,9346, `:472` ×0,9595 — `round01` each | Class factors verbatim from п.18.1.1. |
| **§I п.12** K1 ∉ группа В | **IMPLEMENTED** | inventory applies k1 only to the loaded(N8) leg `:204`; vLeg `:226` carries no k1 | Confirms architecture. |
| **§I п.13** НКО included in scheme rate (no separate НКО line) | **IMPLEMENTED** | No engine adds a separate НКО charge; `tr1-i-belts-*.json` rates are full scheme rates | Adding НКО separately would double-charge. |
| **п.18.2** МВН weight floor `max(actual, МВН)` + over-max per-ton | **IMPLEMENTED** | `tr1-min-weight-norms.json`; over-max per-ton handled in N8 base lookup | The only floor (no general ruble minimum exists in §II). |
| **Индексация / НДС** outside §II, applied last | **IMPLEMENTED** | +10% baked into Прил.N2 base (not re-applied); НДС 22% domestic / 0% export at `computeTariff.ts` VAT | Matches `TARIFF_RULES_EXACT.md` §7. |
| **Приказ п.3** effective-date gate 2026-01-01 | **IMPLEMENTED** | `tr1-rounding-rules.json._meta.regulation` | Primary-source proof now pinned in `prikaz.md`. |

---

## C. Residual NEEDS-VERIFICATION (not money-wrong yet, but unproven)

1. **K3 «с-расстояния» degeneracy.** Engine models нерудный K3=0,77 as flat `× 0,77` from km 1 (`computeTariffN8.ts:444`). If Табл.N4 introduces 0,77 only beyond a threshold km, the full п.16.6 delta formula `rate(L_from) + K3·(rate(L)−rate(L_from))` must be used instead. **Verify the Табл.N4 «с расстояния» column for positions 231–236 cell-by-cell** (chunk [`rulebook/tablitsa-n-4.md`](./rulebook/tablitsa-n-4.md)).
2. **K4 on the порожний leg.** Whether Табл.N5 max-of-two applies to the N25 порожний for own wagons — п.17.1 lists schemes 8/8(1) but порожний schemes N25–29 are addressed separately. `computeInventory.ts:214` applies K4 to the empty leg; confirm against the Табл.N5 applicability list ([`rulebook/tablitsa-n-5.md`](./rulebook/tablitsa-n-5.md)).
3. **пп.22–25 scheme numbering** unresolved between the WebFetch summarizer and `00-index` (рефрижераторные / изотермические / контейнеры / контрейлерные / транспортёры). Only пп.18 and 21 are cross-checked against captured bodies. Affects non-полувагон branches only — read the rendered §II body directly before trusting those branches.

---

## D. Fitted levers the docs do NOT justify (highlight)

| Lever | Where | Status | Verdict |
|---|---|---|---|
| **−754 ₽** flat inventory discount | `computeInventory.ts` `INVENTORY_DISCOUNT = 754` + final `Math.round(... − 754)` | re-cited honestly as R-Тариф «Скидка с общего тарифа на универсальные вагоны»; full provenance comment | **RESOLVED as corroborated-by-oracle (not rule-derived).** Proven FLAT (753,86/754,32 bracket 754 with opposite K4 sign → not a formula); ruled out as Табл.N12/N13/п.28.2 and any §II/п.18 clause. Load-bearing for both inventory oracles. Residual: rule-derivation requires reproducing the И1 combined table offset. See [`INVENTORY_754_RESOLUTION.md`](./INVENTORY_754_RESOLUTION.md) + `TARIFF_LEVER_KILL.md`. |
| **порожний надбавка ×1,1 / ×1,06** | empty-leg multiplier | sourced to Приказ ФАС 999/24 in `TARIFF_RULES_EXACT.md` §6 / `computeInventory.ts` (×1,06) | Has a citation, but the **999/24 надбавка is outside ТР-1** — keep its source pinned and re-verify on each indexation cycle. Not a ТР-1 §II rule. |

**Conversely, rules the docs require that the engine MISSES:** only **Табл.N13 контрейлер reduction wiring** (seed present in `tr1-reductions.json`, but needs Табл.N11 контрейлер base schemes to subtract against) and the **§3 погранстанции** directional coefficients (captured but UNVERIFIED, deliberately not entered). Both are outside the щебень повагонная hot path. Everything else the docs mandate is now implemented on at least the certified + universal contours.
